# Backend API base URL (used by the frontend)
# In development with the Vite proxy, you can leave this empty or set it to:
VITE_API_URL=http://localhost:8000

# NEVER put backend secrets (JWT_SECRET, MONGO_*, AI_API_KEY) here.